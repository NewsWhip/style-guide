import { PathDirective } from './path.directive';

describe('PathDirective', () => {
  it('should create an instance', () => {
    const directive = new PathDirective();
    expect(directive).toBeTruthy();
  });
});
